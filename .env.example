# ─────────────────────────────────────────────────────────────
# Backend 環境變數範本。
# 複製成 .env.local 後填入真實值；.env.local 已被 .gitignore 忽略，
# 絕對不要把真實值寫進這個檔案。
# ─────────────────────────────────────────────────────────────

# PostgreSQL (Supabase transaction pooler, port 6543)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true

# TDX 交通部運輸資料流通服務（即時車位）
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=

# Google Gemini（AI 行程規劃）
GEMINI_API_KEY=

# ── CORS ──────────────────────────────────────────────────────
# 允許跨來源存取的前端網址，必須是完整 origin（協定 + 主機 + 埠），結尾不要加斜線。
# 這【不】是 secret。
#
# 本機開發（Vite）：
FRONTEND_ORIGIN=http://localhost:5173
#
# 正式環境改成實際的前端網域，例如：
#   FRONTEND_ORIGIN=https://your-frontend-domain.com
#
# 未設定時的行為：不開放任何跨來源存取（不會退回 `*`）。
# 此時 API 本身仍可正常使用（curl / server-to-server），只是瀏覽器前端會被擋。
